/**
 * GENERATED — do not edit. Run `make proto`.
 *
 * The Go API, as this codebase consumes it. Generated by
 * `@effect/openapi-generator` from the OpenAPI document grpc-gateway emits from
 * `proto/*.proto`, which is the single source of truth for the gRPC service,
 * the REST reverse proxy, the published spec and this file.
 *
 * `HttpApiBuilder` is never used: no TypeScript implements this. What it gives
 * the browser is a typed client and runtime decoding at the boundary, which is
 * the difference between a wrong response failing here with a readable error and
 * failing three components later as `undefined is not an object`.
 *
 * `scripts/generate-api-client.mjs` describes the two things the document cannot
 * say for itself — that the gateway's `EmitUnpopulated` makes every field
 * present, and that a `format` names a branded id or a 64-bit number.
 */
import * as Schema from "effect/Schema";
import {
  HttpApi,
  HttpApiEndpoint,
  HttpApiGroup,
  HttpApiSchema,
  OpenApi,
} from "effect/unstable/httpapi";
import {
  CentsFromString,
  DriverId,
  ErrorCode,
  FareId,
  Int64FromString,
  RiderId,
  TripId,
} from "./Primitives.js";
// non-recursive definitions
export type V1GetSimulatorResponse = typeof V1GetSimulatorResponse.Type;
export const V1GetSimulatorResponse = Schema.Struct({
  "simulator": Schema.Struct({
    "drivers": Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
    "running": Schema.Number.annotate({
      "description":
        "Running is how many driver goroutines are actually up, which lags\n`drivers` for a moment after a rescale.",
      "format": "int32",
    }).check(Schema.isInt().annotate({ "expected": "an integer" })),
    "pingsTotal": Int64FromString,
    "pingIntervalMs": Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
    "targetPingsPerSecond": Schema.Number.annotate({
      "description": "Drivers divided by the interval: ingest's load, stated.",
      "format": "double",
    }).check(Schema.isFinite().annotate({ "expected": "a finite number" })),
  }),
}).annotate({ "identifier": "v1GetSimulatorResponse" });
export type V1ErrorBody = typeof V1ErrorBody.Type;
export const V1ErrorBody = Schema.Struct({
  "error": Schema.Struct({
    "code": ErrorCode,
    "message": Schema.String.annotate({
      "description":
        "For a human reading a log. Deliberately vague for anything internal — the\ndetail stays in the trace rather than travelling to a browser.",
    }),
  }),
}).annotate({
  "description":
    "The one shape every failure takes on the REST edge.\n\nDeclared here rather than left to grpc-gateway's default because the gateway\ndoes not use that default: `rest.go` installs a custom error handler, and for\nas long as this message was absent the published document described a\n`rpcStatus` — `{code: int, message, details}` — that nothing has ever\nreturned. A generated client believed it, and would have failed to decode\nevery error the server actually sends.",
  "identifier": "v1ErrorBody",
});
export type V1ConfigureSimulatorRequest = typeof V1ConfigureSimulatorRequest.Type;
export const V1ConfigureSimulatorRequest = Schema.Struct({
  "drivers": Schema.Number.annotate({ "format": "int32" }).check(
    Schema.isInt().annotate({ "expected": "an integer" }),
  ),
  "pingIntervalMs": Schema.Number.annotate({ "format": "int32" }).check(
    Schema.isInt().annotate({ "expected": "an integer" }),
  ),
}).annotate({ "identifier": "v1ConfigureSimulatorRequest" });
export type V1ConfigureSimulatorResponse = typeof V1ConfigureSimulatorResponse.Type;
export const V1ConfigureSimulatorResponse = Schema.Struct({
  "simulator": Schema.Struct({
    "drivers": Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
    "running": Schema.Number.annotate({
      "description":
        "Running is how many driver goroutines are actually up, which lags\n`drivers` for a moment after a rescale.",
      "format": "int32",
    }).check(Schema.isInt().annotate({ "expected": "an integer" })),
    "pingsTotal": Int64FromString,
    "pingIntervalMs": Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
    "targetPingsPerSecond": Schema.Number.annotate({
      "description": "Drivers divided by the interval: ingest's load, stated.",
      "format": "double",
    }).check(Schema.isFinite().annotate({ "expected": "a finite number" })),
  }),
}).annotate({ "identifier": "v1ConfigureSimulatorResponse" });
export type V1ListTripsResponse = typeof V1ListTripsResponse.Type;
export const V1ListTripsResponse = Schema.Struct({
  "trips": Schema.Array(Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  })),
  "nextPageToken": Schema.String.annotate({ "description": "Empty when there are no more." }),
}).annotate({ "identifier": "v1ListTripsResponse" });
export type V1CreateTripRequest = typeof V1CreateTripRequest.Type;
export const V1CreateTripRequest = Schema.Struct({
  "fareId": FareId,
  "idempotencyKey": Schema.String.annotate({
    "description":
      "Generated client-side, so a retry over a flaky connection is recognised\nrather than booked twice.\n\nThe generated TypeScript client sends it here, in the message. The gateway\nalso accepts an `Idempotency-Key` header and copies it into this field, for\ncallers that would rather describe the request than the trip — a proxy can\nread a header without parsing a body. The handler prefers the field.",
  }),
}).annotate({ "identifier": "v1CreateTripRequest" });
export type V1CreateTripResponse = typeof V1CreateTripResponse.Type;
export const V1CreateTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1CreateTripResponse" });
export type V1GetTripResponse = typeof V1GetTripResponse.Type;
export const V1GetTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1GetTripResponse" });
export type V1ArriveTripResponse = typeof V1ArriveTripResponse.Type;
export const V1ArriveTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1ArriveTripResponse" });
export type TripServiceCancelTripBody = typeof TripServiceCancelTripBody.Type;
export const TripServiceCancelTripBody = Schema.Struct({ "reason": Schema.String }).annotate({
  "identifier": "TripServiceCancelTripBody",
});
export type V1CancelTripResponse = typeof V1CancelTripResponse.Type;
export const V1CancelTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1CancelTripResponse" });
export type V1CompleteTripResponse = typeof V1CompleteTripResponse.Type;
export const V1CompleteTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1CompleteTripResponse" });
export type V1StartTripResponse = typeof V1StartTripResponse.Type;
export const V1StartTripResponse = Schema.Struct({
  "trip": Schema.Struct({
    "id": TripId,
    "riderId": RiderId,
    "driverId": DriverId,
    "status": Schema.Literals([
      "TRIP_STATUS_UNSPECIFIED",
      "TRIP_STATUS_REQUESTED",
      "TRIP_STATUS_OFFERED",
      "TRIP_STATUS_ACCEPTED",
      "TRIP_STATUS_ARRIVED",
      "TRIP_STATUS_IN_PROGRESS",
      "TRIP_STATUS_COMPLETED",
      "TRIP_STATUS_CANCELLED",
      "TRIP_STATUS_UNMATCHED",
    ]).annotate({
      "description":
        "TripStatus is the state machine, and the wire is the place it is written\ndown once. UNSPECIFIED is reserved by convention so an unset field is not\nsilently a valid state.\n\n - TRIP_STATUS_UNMATCHED: No driver was found before the request expired. Distinct from cancelled:\nnobody chose it, and the rider should be offered a retry rather than an\napology.",
      "default": "TRIP_STATUS_UNSPECIFIED",
    }),
    "pickup": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "dropoff": Schema.Struct({
      "lat": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "lng": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
    }).annotate({
      "description":
        "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
    }),
    "route": Schema.Struct({
      "polyline6": Schema.String.annotate({
        "description":
          "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
      }),
      "meters": Schema.Number.annotate({ "format": "double" }).check(
        Schema.isFinite().annotate({ "expected": "a finite number" }),
      ),
      "seconds": Int64FromString,
    }).annotate({ "description": "Route is a driveable path with its cost." }),
    "totalCents": CentsFromString,
    "createdAt": Schema.String.annotate({ "format": "date-time" }),
    "updatedAt": Schema.String.annotate({ "format": "date-time" }),
  }),
}).annotate({ "identifier": "v1StartTripResponse" });
export type V1PreviewTripRequest = typeof V1PreviewTripRequest.Type;
export const V1PreviewTripRequest = Schema.Struct({
  "pickup": Schema.Struct({
    "lat": Schema.Number.annotate({ "format": "double" }).check(
      Schema.isFinite().annotate({ "expected": "a finite number" }),
    ),
    "lng": Schema.Number.annotate({ "format": "double" }).check(
      Schema.isFinite().annotate({ "expected": "a finite number" }),
    ),
  }).annotate({
    "description":
      "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
  }),
  "dropoff": Schema.Struct({
    "lat": Schema.Number.annotate({ "format": "double" }).check(
      Schema.isFinite().annotate({ "expected": "a finite number" }),
    ),
    "lng": Schema.Number.annotate({ "format": "double" }).check(
      Schema.isFinite().annotate({ "expected": "a finite number" }),
    ),
  }).annotate({
    "description":
      "Coordinate is WGS84. `lng` rather than `lon`, matching H3, Valhalla and the\nrest of this codebase — one spelling everywhere is worth more than any\nargument about which.",
  }),
}).annotate({
  "description":
    "The caller is NOT a field on any request.\n\nIt arrives as gRPC metadata, put there by the gateway from a verified token.\nThis matters more now that the REST body maps straight onto the message: a\n`rider_id` field would be client-supplied, and a client that can name the\nrider can quote, book and read rides as somebody else.",
  "identifier": "v1PreviewTripRequest",
});
export type V1PreviewTripResponse = typeof V1PreviewTripResponse.Type;
export const V1PreviewTripResponse = Schema.Struct({
  "fares": Schema.Array(Schema.Struct({
    "fareId": FareId,
    "packageSlug": Schema.String.annotate({
      "description":
        "Sedan, van and so on. A string rather than an enum because the catalogue is\noperator configuration, not a protocol change.",
    }),
    "totalCents": CentsFromString,
    "surgeMultiplier": Schema.Number.annotate({
      "description":
        "The multiplier applied for demand in the pickup cell at quote time. Carried\nexplicitly so a rider can be shown why a fare is what it is.",
      "format": "double",
    }).check(Schema.isFinite().annotate({ "expected": "a finite number" })),
    "expiresAt": Schema.String.annotate({ "format": "date-time" }),
  })).annotate({ "description": "Quotes for each vehicle class, priced from the same route." }),
  "route": Schema.Struct({
    "polyline6": Schema.String.annotate({
      "description":
        "Polyline encoded at precision 6, as Valhalla emits it. Not a repeated\nCoordinate: a city route is hundreds of points, and the encoded form is\nroughly a tenth the size on a connection a phone is paying for.",
    }),
    "meters": Schema.Number.annotate({ "format": "double" }).check(
      Schema.isFinite().annotate({ "expected": "a finite number" }),
    ),
    "seconds": Int64FromString,
  }).annotate({ "description": "Route is a driveable path with its cost." }),
}).annotate({ "identifier": "v1PreviewTripResponse" });
// schemas
export type SimulatorGet200 = typeof SimulatorGet200.Type;
export const SimulatorGet200 = V1GetSimulatorResponse;
export type SimulatorGet400 = typeof SimulatorGet400.Type;
export const SimulatorGet400 = V1ErrorBody;
export type SimulatorGet401 = typeof SimulatorGet401.Type;
export const SimulatorGet401 = V1ErrorBody;
export type SimulatorGet403 = typeof SimulatorGet403.Type;
export const SimulatorGet403 = V1ErrorBody;
export type SimulatorGet404 = typeof SimulatorGet404.Type;
export const SimulatorGet404 = V1ErrorBody;
export type SimulatorGet409 = typeof SimulatorGet409.Type;
export const SimulatorGet409 = V1ErrorBody;
export type SimulatorGet429 = typeof SimulatorGet429.Type;
export const SimulatorGet429 = V1ErrorBody;
export type SimulatorGet500 = typeof SimulatorGet500.Type;
export const SimulatorGet500 = V1ErrorBody;
export type SimulatorGet501 = typeof SimulatorGet501.Type;
export const SimulatorGet501 = V1ErrorBody;
export type SimulatorGet503 = typeof SimulatorGet503.Type;
export const SimulatorGet503 = V1ErrorBody;
export type SimulatorGet504 = typeof SimulatorGet504.Type;
export const SimulatorGet504 = V1ErrorBody;
export type SimulatorConfigureRequestJson = typeof SimulatorConfigureRequestJson.Type;
export const SimulatorConfigureRequestJson = V1ConfigureSimulatorRequest;
export type SimulatorConfigure200 = typeof SimulatorConfigure200.Type;
export const SimulatorConfigure200 = V1ConfigureSimulatorResponse;
export type SimulatorConfigure400 = typeof SimulatorConfigure400.Type;
export const SimulatorConfigure400 = V1ErrorBody;
export type SimulatorConfigure401 = typeof SimulatorConfigure401.Type;
export const SimulatorConfigure401 = V1ErrorBody;
export type SimulatorConfigure403 = typeof SimulatorConfigure403.Type;
export const SimulatorConfigure403 = V1ErrorBody;
export type SimulatorConfigure404 = typeof SimulatorConfigure404.Type;
export const SimulatorConfigure404 = V1ErrorBody;
export type SimulatorConfigure409 = typeof SimulatorConfigure409.Type;
export const SimulatorConfigure409 = V1ErrorBody;
export type SimulatorConfigure429 = typeof SimulatorConfigure429.Type;
export const SimulatorConfigure429 = V1ErrorBody;
export type SimulatorConfigure500 = typeof SimulatorConfigure500.Type;
export const SimulatorConfigure500 = V1ErrorBody;
export type SimulatorConfigure501 = typeof SimulatorConfigure501.Type;
export const SimulatorConfigure501 = V1ErrorBody;
export type SimulatorConfigure503 = typeof SimulatorConfigure503.Type;
export const SimulatorConfigure503 = V1ErrorBody;
export type SimulatorConfigure504 = typeof SimulatorConfigure504.Type;
export const SimulatorConfigure504 = V1ErrorBody;
export type TripsListParams = typeof TripsListParams.Type;
export const TripsListParams = Schema.Struct({
  "pageSize": Schema.optionalKey(
    Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
  ),
  "pageToken": Schema.optionalKey(Schema.String),
  "status": Schema.optionalKey(Schema.String),
});
export type TripsListQuery = typeof TripsListQuery.Type;
export const TripsListQuery = Schema.Struct({
  "pageSize": Schema.optionalKey(
    Schema.Number.annotate({ "format": "int32" }).check(
      Schema.isInt().annotate({ "expected": "an integer" }),
    ),
  ),
  "pageToken": Schema.optionalKey(Schema.String),
  "status": Schema.optionalKey(Schema.String),
});
export type TripsList200 = typeof TripsList200.Type;
export const TripsList200 = V1ListTripsResponse;
export type TripsList400 = typeof TripsList400.Type;
export const TripsList400 = V1ErrorBody;
export type TripsList401 = typeof TripsList401.Type;
export const TripsList401 = V1ErrorBody;
export type TripsList403 = typeof TripsList403.Type;
export const TripsList403 = V1ErrorBody;
export type TripsList404 = typeof TripsList404.Type;
export const TripsList404 = V1ErrorBody;
export type TripsList409 = typeof TripsList409.Type;
export const TripsList409 = V1ErrorBody;
export type TripsList429 = typeof TripsList429.Type;
export const TripsList429 = V1ErrorBody;
export type TripsList500 = typeof TripsList500.Type;
export const TripsList500 = V1ErrorBody;
export type TripsList501 = typeof TripsList501.Type;
export const TripsList501 = V1ErrorBody;
export type TripsList503 = typeof TripsList503.Type;
export const TripsList503 = V1ErrorBody;
export type TripsList504 = typeof TripsList504.Type;
export const TripsList504 = V1ErrorBody;
export type TripsCreateRequestJson = typeof TripsCreateRequestJson.Type;
export const TripsCreateRequestJson = V1CreateTripRequest;
export type TripsCreate200 = typeof TripsCreate200.Type;
export const TripsCreate200 = V1CreateTripResponse;
export type TripsCreate400 = typeof TripsCreate400.Type;
export const TripsCreate400 = V1ErrorBody;
export type TripsCreate401 = typeof TripsCreate401.Type;
export const TripsCreate401 = V1ErrorBody;
export type TripsCreate403 = typeof TripsCreate403.Type;
export const TripsCreate403 = V1ErrorBody;
export type TripsCreate404 = typeof TripsCreate404.Type;
export const TripsCreate404 = V1ErrorBody;
export type TripsCreate409 = typeof TripsCreate409.Type;
export const TripsCreate409 = V1ErrorBody;
export type TripsCreate429 = typeof TripsCreate429.Type;
export const TripsCreate429 = V1ErrorBody;
export type TripsCreate500 = typeof TripsCreate500.Type;
export const TripsCreate500 = V1ErrorBody;
export type TripsCreate501 = typeof TripsCreate501.Type;
export const TripsCreate501 = V1ErrorBody;
export type TripsCreate503 = typeof TripsCreate503.Type;
export const TripsCreate503 = V1ErrorBody;
export type TripsCreate504 = typeof TripsCreate504.Type;
export const TripsCreate504 = V1ErrorBody;
export type TripsGetPathParams = typeof TripsGetPathParams.Type;
export const TripsGetPathParams = Schema.Struct({ "tripId": TripId });
export type TripsGet200 = typeof TripsGet200.Type;
export const TripsGet200 = V1GetTripResponse;
export type TripsGet400 = typeof TripsGet400.Type;
export const TripsGet400 = V1ErrorBody;
export type TripsGet401 = typeof TripsGet401.Type;
export const TripsGet401 = V1ErrorBody;
export type TripsGet403 = typeof TripsGet403.Type;
export const TripsGet403 = V1ErrorBody;
export type TripsGet404 = typeof TripsGet404.Type;
export const TripsGet404 = V1ErrorBody;
export type TripsGet409 = typeof TripsGet409.Type;
export const TripsGet409 = V1ErrorBody;
export type TripsGet429 = typeof TripsGet429.Type;
export const TripsGet429 = V1ErrorBody;
export type TripsGet500 = typeof TripsGet500.Type;
export const TripsGet500 = V1ErrorBody;
export type TripsGet501 = typeof TripsGet501.Type;
export const TripsGet501 = V1ErrorBody;
export type TripsGet503 = typeof TripsGet503.Type;
export const TripsGet503 = V1ErrorBody;
export type TripsGet504 = typeof TripsGet504.Type;
export const TripsGet504 = V1ErrorBody;
export type TripsArrivePathParams = typeof TripsArrivePathParams.Type;
export const TripsArrivePathParams = Schema.Struct({ "tripId": TripId });
export type TripsArrive200 = typeof TripsArrive200.Type;
export const TripsArrive200 = V1ArriveTripResponse;
export type TripsArrive400 = typeof TripsArrive400.Type;
export const TripsArrive400 = V1ErrorBody;
export type TripsArrive401 = typeof TripsArrive401.Type;
export const TripsArrive401 = V1ErrorBody;
export type TripsArrive403 = typeof TripsArrive403.Type;
export const TripsArrive403 = V1ErrorBody;
export type TripsArrive404 = typeof TripsArrive404.Type;
export const TripsArrive404 = V1ErrorBody;
export type TripsArrive409 = typeof TripsArrive409.Type;
export const TripsArrive409 = V1ErrorBody;
export type TripsArrive429 = typeof TripsArrive429.Type;
export const TripsArrive429 = V1ErrorBody;
export type TripsArrive500 = typeof TripsArrive500.Type;
export const TripsArrive500 = V1ErrorBody;
export type TripsArrive501 = typeof TripsArrive501.Type;
export const TripsArrive501 = V1ErrorBody;
export type TripsArrive503 = typeof TripsArrive503.Type;
export const TripsArrive503 = V1ErrorBody;
export type TripsArrive504 = typeof TripsArrive504.Type;
export const TripsArrive504 = V1ErrorBody;
export type TripsCancelPathParams = typeof TripsCancelPathParams.Type;
export const TripsCancelPathParams = Schema.Struct({ "tripId": TripId });
export type TripsCancelRequestJson = typeof TripsCancelRequestJson.Type;
export const TripsCancelRequestJson = TripServiceCancelTripBody;
export type TripsCancel200 = typeof TripsCancel200.Type;
export const TripsCancel200 = V1CancelTripResponse;
export type TripsCancel400 = typeof TripsCancel400.Type;
export const TripsCancel400 = V1ErrorBody;
export type TripsCancel401 = typeof TripsCancel401.Type;
export const TripsCancel401 = V1ErrorBody;
export type TripsCancel403 = typeof TripsCancel403.Type;
export const TripsCancel403 = V1ErrorBody;
export type TripsCancel404 = typeof TripsCancel404.Type;
export const TripsCancel404 = V1ErrorBody;
export type TripsCancel409 = typeof TripsCancel409.Type;
export const TripsCancel409 = V1ErrorBody;
export type TripsCancel429 = typeof TripsCancel429.Type;
export const TripsCancel429 = V1ErrorBody;
export type TripsCancel500 = typeof TripsCancel500.Type;
export const TripsCancel500 = V1ErrorBody;
export type TripsCancel501 = typeof TripsCancel501.Type;
export const TripsCancel501 = V1ErrorBody;
export type TripsCancel503 = typeof TripsCancel503.Type;
export const TripsCancel503 = V1ErrorBody;
export type TripsCancel504 = typeof TripsCancel504.Type;
export const TripsCancel504 = V1ErrorBody;
export type TripsCompletePathParams = typeof TripsCompletePathParams.Type;
export const TripsCompletePathParams = Schema.Struct({ "tripId": TripId });
export type TripsComplete200 = typeof TripsComplete200.Type;
export const TripsComplete200 = V1CompleteTripResponse;
export type TripsComplete400 = typeof TripsComplete400.Type;
export const TripsComplete400 = V1ErrorBody;
export type TripsComplete401 = typeof TripsComplete401.Type;
export const TripsComplete401 = V1ErrorBody;
export type TripsComplete403 = typeof TripsComplete403.Type;
export const TripsComplete403 = V1ErrorBody;
export type TripsComplete404 = typeof TripsComplete404.Type;
export const TripsComplete404 = V1ErrorBody;
export type TripsComplete409 = typeof TripsComplete409.Type;
export const TripsComplete409 = V1ErrorBody;
export type TripsComplete429 = typeof TripsComplete429.Type;
export const TripsComplete429 = V1ErrorBody;
export type TripsComplete500 = typeof TripsComplete500.Type;
export const TripsComplete500 = V1ErrorBody;
export type TripsComplete501 = typeof TripsComplete501.Type;
export const TripsComplete501 = V1ErrorBody;
export type TripsComplete503 = typeof TripsComplete503.Type;
export const TripsComplete503 = V1ErrorBody;
export type TripsComplete504 = typeof TripsComplete504.Type;
export const TripsComplete504 = V1ErrorBody;
export type TripsStartPathParams = typeof TripsStartPathParams.Type;
export const TripsStartPathParams = Schema.Struct({ "tripId": TripId });
export type TripsStart200 = typeof TripsStart200.Type;
export const TripsStart200 = V1StartTripResponse;
export type TripsStart400 = typeof TripsStart400.Type;
export const TripsStart400 = V1ErrorBody;
export type TripsStart401 = typeof TripsStart401.Type;
export const TripsStart401 = V1ErrorBody;
export type TripsStart403 = typeof TripsStart403.Type;
export const TripsStart403 = V1ErrorBody;
export type TripsStart404 = typeof TripsStart404.Type;
export const TripsStart404 = V1ErrorBody;
export type TripsStart409 = typeof TripsStart409.Type;
export const TripsStart409 = V1ErrorBody;
export type TripsStart429 = typeof TripsStart429.Type;
export const TripsStart429 = V1ErrorBody;
export type TripsStart500 = typeof TripsStart500.Type;
export const TripsStart500 = V1ErrorBody;
export type TripsStart501 = typeof TripsStart501.Type;
export const TripsStart501 = V1ErrorBody;
export type TripsStart503 = typeof TripsStart503.Type;
export const TripsStart503 = V1ErrorBody;
export type TripsStart504 = typeof TripsStart504.Type;
export const TripsStart504 = V1ErrorBody;
export type TripsPreviewRequestJson = typeof TripsPreviewRequestJson.Type;
export const TripsPreviewRequestJson = V1PreviewTripRequest;
export type TripsPreview200 = typeof TripsPreview200.Type;
export const TripsPreview200 = V1PreviewTripResponse;
export type TripsPreview400 = typeof TripsPreview400.Type;
export const TripsPreview400 = V1ErrorBody;
export type TripsPreview401 = typeof TripsPreview401.Type;
export const TripsPreview401 = V1ErrorBody;
export type TripsPreview403 = typeof TripsPreview403.Type;
export const TripsPreview403 = V1ErrorBody;
export type TripsPreview404 = typeof TripsPreview404.Type;
export const TripsPreview404 = V1ErrorBody;
export type TripsPreview409 = typeof TripsPreview409.Type;
export const TripsPreview409 = V1ErrorBody;
export type TripsPreview429 = typeof TripsPreview429.Type;
export const TripsPreview429 = V1ErrorBody;
export type TripsPreview500 = typeof TripsPreview500.Type;
export const TripsPreview500 = V1ErrorBody;
export type TripsPreview501 = typeof TripsPreview501.Type;
export const TripsPreview501 = V1ErrorBody;
export type TripsPreview503 = typeof TripsPreview503.Type;
export const TripsPreview503 = V1ErrorBody;
export type TripsPreview504 = typeof TripsPreview504.Type;
export const TripsPreview504 = V1ErrorBody;

class SimulatorGroup extends HttpApiGroup.make("simulator")
  .add(
    HttpApiEndpoint.get("get", "/v1/simulator", {
      success: SimulatorGet200,
      error: [
        SimulatorGet400.pipe(HttpApiSchema.status(400)),
        SimulatorGet401.pipe(HttpApiSchema.status(401)),
        SimulatorGet403.pipe(HttpApiSchema.status(403)),
        SimulatorGet404.pipe(HttpApiSchema.status(404)),
        SimulatorGet409.pipe(HttpApiSchema.status(409)),
        SimulatorGet429.pipe(HttpApiSchema.status(429)),
        SimulatorGet500,
        SimulatorGet501.pipe(HttpApiSchema.status(501)),
        SimulatorGet503.pipe(HttpApiSchema.status(503)),
        SimulatorGet504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "get")
      .annotate(
        OpenApi.Summary,
        "GetSimulator is the fleet's size and rate, as configured and as running.",
      ),
    HttpApiEndpoint.put("configure", "/v1/simulator", {
      payload: SimulatorConfigureRequestJson,
      success: SimulatorConfigure200,
      error: [
        SimulatorConfigure400.pipe(HttpApiSchema.status(400)),
        SimulatorConfigure401.pipe(HttpApiSchema.status(401)),
        SimulatorConfigure403.pipe(HttpApiSchema.status(403)),
        SimulatorConfigure404.pipe(HttpApiSchema.status(404)),
        SimulatorConfigure409.pipe(HttpApiSchema.status(409)),
        SimulatorConfigure429.pipe(HttpApiSchema.status(429)),
        SimulatorConfigure500,
        SimulatorConfigure501.pipe(HttpApiSchema.status(501)),
        SimulatorConfigure503.pipe(HttpApiSchema.status(503)),
        SimulatorConfigure504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "configure")
      .annotate(OpenApi.Summary, "ConfigureSimulator rescales the fleet.")
      .annotate(
        OpenApi.Description,
        "The whole desired configuration rather than a partial patch: the console\nshows both values and sends both, and a request that must carry every\nfield is one the generated client can type as exactly that.",
      ),
  )
{}

class TripsGroup extends HttpApiGroup.make("trips")
  .add(
    HttpApiEndpoint.get("list", "/v1/trips", {
      query: TripsListQuery,
      success: TripsList200,
      error: [
        TripsList400.pipe(HttpApiSchema.status(400)),
        TripsList401.pipe(HttpApiSchema.status(401)),
        TripsList403.pipe(HttpApiSchema.status(403)),
        TripsList404.pipe(HttpApiSchema.status(404)),
        TripsList409.pipe(HttpApiSchema.status(409)),
        TripsList429.pipe(HttpApiSchema.status(429)),
        TripsList500,
        TripsList501.pipe(HttpApiSchema.status(501)),
        TripsList503.pipe(HttpApiSchema.status(503)),
        TripsList504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "list")
      .annotate(
        OpenApi.Summary,
        "ListTrips is the caller's history, newest first — the rides a rider booked,\nor the rides a driver was assigned. The token decides which.",
      ),
    HttpApiEndpoint.post("create", "/v1/trips", {
      payload: TripsCreateRequestJson,
      success: TripsCreate200,
      error: [
        TripsCreate400.pipe(HttpApiSchema.status(400)),
        TripsCreate401.pipe(HttpApiSchema.status(401)),
        TripsCreate403.pipe(HttpApiSchema.status(403)),
        TripsCreate404.pipe(HttpApiSchema.status(404)),
        TripsCreate409.pipe(HttpApiSchema.status(409)),
        TripsCreate429.pipe(HttpApiSchema.status(429)),
        TripsCreate500,
        TripsCreate501.pipe(HttpApiSchema.status(501)),
        TripsCreate503.pipe(HttpApiSchema.status(503)),
        TripsCreate504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "create")
      .annotate(
        OpenApi.Summary,
        "Create commits a previewed fare to a real trip and starts matching.",
      )
      .annotate(
        OpenApi.Description,
        "Takes an idempotency key because the caller is a browser on a phone\nnetwork: a retried request must return the original trip rather than\nbooking a second one.",
      ),
    HttpApiEndpoint.get("get", "/v1/trips/:tripId", {
      params: TripsGetPathParams,
      success: TripsGet200,
      error: [
        TripsGet400.pipe(HttpApiSchema.status(400)),
        TripsGet401.pipe(HttpApiSchema.status(401)),
        TripsGet403.pipe(HttpApiSchema.status(403)),
        TripsGet404.pipe(HttpApiSchema.status(404)),
        TripsGet409.pipe(HttpApiSchema.status(409)),
        TripsGet429.pipe(HttpApiSchema.status(429)),
        TripsGet500,
        TripsGet501.pipe(HttpApiSchema.status(501)),
        TripsGet503.pipe(HttpApiSchema.status(503)),
        TripsGet504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "get")
      .annotate(OpenApi.Summary, "Get returns a trip's current state."),
    HttpApiEndpoint.post("arrive", "/v1/trips/:tripId/arrive", {
      params: TripsArrivePathParams,
      success: TripsArrive200,
      error: [
        TripsArrive400.pipe(HttpApiSchema.status(400)),
        TripsArrive401.pipe(HttpApiSchema.status(401)),
        TripsArrive403.pipe(HttpApiSchema.status(403)),
        TripsArrive404.pipe(HttpApiSchema.status(404)),
        TripsArrive409.pipe(HttpApiSchema.status(409)),
        TripsArrive429.pipe(HttpApiSchema.status(429)),
        TripsArrive500,
        TripsArrive501.pipe(HttpApiSchema.status(501)),
        TripsArrive503.pipe(HttpApiSchema.status(503)),
        TripsArrive504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "arrive")
      .annotate(
        OpenApi.Summary,
        "ArriveTrip, StartTrip and CompleteTrip are the driver's half of the\nlifecycle: accepted → arrived → in progress → completed.",
      )
      .annotate(
        OpenApi.Description,
        "Only the driver the trip is assigned to may call them, and the trip service\nenforces that rather than the gateway, for the same reason ownership is\nenforced there on reads: only the service knows whose trip it is. A caller\nwho is not the assigned driver gets NotFound, because confirming a trip\nexists to someone who may not touch it is itself a disclosure.\n\nNo body: the trip id in the path is the whole request, and the caller is\nmetadata like everywhere else.\nArrive: the driver is at the pickup.",
      ),
    HttpApiEndpoint.post("cancel", "/v1/trips/:tripId/cancel", {
      params: TripsCancelPathParams,
      payload: TripsCancelRequestJson,
      success: TripsCancel200,
      error: [
        TripsCancel400.pipe(HttpApiSchema.status(400)),
        TripsCancel401.pipe(HttpApiSchema.status(401)),
        TripsCancel403.pipe(HttpApiSchema.status(403)),
        TripsCancel404.pipe(HttpApiSchema.status(404)),
        TripsCancel409.pipe(HttpApiSchema.status(409)),
        TripsCancel429.pipe(HttpApiSchema.status(429)),
        TripsCancel500,
        TripsCancel501.pipe(HttpApiSchema.status(501)),
        TripsCancel503.pipe(HttpApiSchema.status(503)),
        TripsCancel504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "cancel")
      .annotate(OpenApi.Summary, "Cancel ends a trip before completion.")
      .annotate(
        OpenApi.Description,
        "A custom method rather than DELETE: cancelling is a state transition that\nreturns the trip, not a deletion — the row stays, and a rider can still\nread what happened to it.",
      ),
    HttpApiEndpoint.post("complete", "/v1/trips/:tripId/complete", {
      params: TripsCompletePathParams,
      success: TripsComplete200,
      error: [
        TripsComplete400.pipe(HttpApiSchema.status(400)),
        TripsComplete401.pipe(HttpApiSchema.status(401)),
        TripsComplete403.pipe(HttpApiSchema.status(403)),
        TripsComplete404.pipe(HttpApiSchema.status(404)),
        TripsComplete409.pipe(HttpApiSchema.status(409)),
        TripsComplete429.pipe(HttpApiSchema.status(429)),
        TripsComplete500,
        TripsComplete501.pipe(HttpApiSchema.status(501)),
        TripsComplete503.pipe(HttpApiSchema.status(503)),
        TripsComplete504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "complete")
      .annotate(OpenApi.Summary, "Complete: the rider has been dropped off."),
    HttpApiEndpoint.post("start", "/v1/trips/:tripId/start", {
      params: TripsStartPathParams,
      success: TripsStart200,
      error: [
        TripsStart400.pipe(HttpApiSchema.status(400)),
        TripsStart401.pipe(HttpApiSchema.status(401)),
        TripsStart403.pipe(HttpApiSchema.status(403)),
        TripsStart404.pipe(HttpApiSchema.status(404)),
        TripsStart409.pipe(HttpApiSchema.status(409)),
        TripsStart429.pipe(HttpApiSchema.status(429)),
        TripsStart500,
        TripsStart501.pipe(HttpApiSchema.status(501)),
        TripsStart503.pipe(HttpApiSchema.status(503)),
        TripsStart504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "start")
      .annotate(OpenApi.Summary, "Start: the rider is in the car."),
    HttpApiEndpoint.post("preview", "/v1/trips:preview", {
      payload: TripsPreviewRequestJson,
      success: TripsPreview200,
      error: [
        TripsPreview400.pipe(HttpApiSchema.status(400)),
        TripsPreview401.pipe(HttpApiSchema.status(401)),
        TripsPreview403.pipe(HttpApiSchema.status(403)),
        TripsPreview404.pipe(HttpApiSchema.status(404)),
        TripsPreview409.pipe(HttpApiSchema.status(409)),
        TripsPreview429.pipe(HttpApiSchema.status(429)),
        TripsPreview500,
        TripsPreview501.pipe(HttpApiSchema.status(501)),
        TripsPreview503.pipe(HttpApiSchema.status(503)),
        TripsPreview504.pipe(HttpApiSchema.status(504)),
      ],
    })
      .annotate(OpenApi.Identifier, "preview")
      .annotate(
        OpenApi.Summary,
        "Preview quotes a trip without committing to it. Read-only, so a rider\ndragging a pin can call it repeatedly.",
      )
      .annotate(
        OpenApi.Description,
        "POST rather than GET despite being read-only: the request carries two\ncoordinate pairs, and a GET would put a rider's exact pickup and dropoff\ninto every access log and browser history along the way.",
      ),
  )
{}

export class SurgeApi extends HttpApi.make("SurgeApi")
  .annotate(OpenApi.Title, "trip.proto")
  .annotate(OpenApi.Version, "version not set")
  .add(SimulatorGroup, TripsGroup)
{}
