// Expo's globals — `process.env.EXPO_PUBLIC_*` above all, which Metro inlines.
// What `expo start` would write to `expo-env.d.ts`, committed so `tsc -b` in CI
// does not depend on the dev server having run.
/// <reference types="expo/types" />
