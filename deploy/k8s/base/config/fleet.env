# FLEET_IDENTITY_PROVIDER and FLEET_DOCUMENT_STORE have no defaults and are
# chosen per environment, for the same reason chat's push provider is: the one
# that approves drivers nobody checked is the worst of the three.
FLEET_GRPC_LISTEN=:8115
FLEET_METRICS_ADDR=:9110
FLEET_SWEEP_INTERVAL=1h
