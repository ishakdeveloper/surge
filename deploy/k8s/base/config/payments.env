# PAYMENTS_PROCESSOR is left to its default, stripe, so a deployment that forgets
# to choose fails for want of a key rather than charging nobody. Development
# chooses fake.
PAYMENTS_GRPC_LISTEN=:8112
PAYMENTS_METRICS_ADDR=:9107
