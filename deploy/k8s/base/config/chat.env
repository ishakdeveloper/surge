# CHAT_PUSH_PROVIDER has no default and is chosen per environment: a deploy that
# forgets fails at boot rather than running while nobody's phone ever buzzes.
CHAT_GRPC_LISTEN=:8113
CHAT_METRICS_ADDR=:9108
TRIP_GRPC_ADDR=trip:8110
